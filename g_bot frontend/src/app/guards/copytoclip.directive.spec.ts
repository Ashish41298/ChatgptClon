import { CopytoclipDirective } from './copytoclip.directive';

describe('CopytoclipDirective', () => {
  it('should create an instance', () => {
    const directive = new CopytoclipDirective();
    expect(directive).toBeTruthy();
  });
});
